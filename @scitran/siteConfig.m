function config = siteConfig(obj)
% Return site configuration information
%
% 
% Wandell, Vistasoft, 2018

config = obj.fw.getConfig;

% In the future, we can set up some parameters to printout out a nice
% representation of the site configuration.

end